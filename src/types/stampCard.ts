export interface StampCardConfig {
  totalStamps: number;
  minSpendPerStamp: number;
  reward: string;
  terms: string;
  eventName: string;
  eventLocation: string;
  startDate: string;
  endDate: string;
}